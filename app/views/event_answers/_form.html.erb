<%= form_for(@event_answer) do |f| %>
  <% if @event_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_answer.errors.count, "error") %> prohibited this event_answer from being saved:</h2>

      <ul>
      <% @event_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :answer %><br>
    <%= f.text_field :answer %>
  </div>
  <div class="field">
    <%= f.label :next_type %><br>
    <%= f.text_field :next_type %>
  </div>
  <div class="field">
    <%= f.label :next_id %><br>
    <%= f.number_field :next_id %>
  </div>
  <div class="field">
    <%= f.label :event_part_id %><br />
    <%= f.select :event_part_id, options_for_select(@event_parts.map{ |s| [s.title, s.id] }) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
