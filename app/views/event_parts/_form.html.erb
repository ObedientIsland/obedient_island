<%= form_for(@event_part) do |f| %>
  <% if @event_part.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_part.errors.count, "error") %> prohibited this event_part from being saved:</h2>

      <ul>
      <% @event_part.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_area :text %>
  </div>
  <div class="field">
    <%= f.label :next_type %><br />
    <%= f.text_field :next_type %>
  </div>
  <div class="field">
    <%= f.label :next_id %><br />
    <%= f.text_field :next_id %>
  </div>
  <div class="field">
    <%= f.label :image %><br />
    <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label :event_id %><br />
    <%= f.select :event_id, options_for_select(@events.map{ |s| [s.name, s.id] }) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
